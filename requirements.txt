# Core pip-installable dependencies
# NOTE: SCIP and CPLEX must be installed via conda (see environment.yml)
numpy>=1.26
pyomo>=6.9
qubolite>=0.7,<0.8      # 0.8+ has a C extension that fails on Windows MSVC
notebook>=7.0
ipykernel>=6.29